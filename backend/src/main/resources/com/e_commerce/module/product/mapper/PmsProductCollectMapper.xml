<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.e_commerce.module.product.mapper.PmsProductCollectMapper">

    <!-- 根据用户ID查询收藏列表（关联商品+SKU） -->
    <select id="getCollectsByUserId" resultMap="CollectWithSkuMap">
        SELECT
        pc.id AS collectId,
        pc.product_id AS productId,
        pc.create_time AS createTime,
        p.product_name AS productName,
        p.pic AS pic,
        s.id AS sku_id,
        s.price,
        s.sku_code
        FROM pms_product_collect pc
        LEFT JOIN pms_product p ON pc.product_id = p.id
        LEFT JOIN pms_sku s ON p.id = s.product_id
        WHERE pc.user_id = #{userId}
        ORDER BY pc.create_time DESC
    </select>

    <!-- 结果映射：收藏 + 商品 + SKU列表 -->
    <resultMap id="CollectWithSkuMap" type="com.e_commerce.module.product.vo.PmsCollectVo">
        <id property="collectId" column="collectId"/>
        <result property="productId" column="productId"/>
        <result property="productName" column="productName"/>
        <result property="pic" column="pic"/>
        <result property="createTime" column="createTime"/>
        <collection property="skuList" ofType="com.e_commerce.module.product.vo.PmsSkuVo">
            <id property="id" column="sku_id"/>
            <result property="price" column="price"/>
            <result property="skuCode" column="sku_code"/>
        </collection>
    </resultMap>
</mapper>
